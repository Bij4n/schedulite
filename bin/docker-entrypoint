#!/bin/bash -e

# Migrations run via Render's preDeployCommand, NOT here.
# Running db:prepare in the entrypoint races multiple instances on
# rolling deploys and fails when DATABASE_URL hasn't propagated yet.

exec "${@}"
